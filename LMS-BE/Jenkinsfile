pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                sh 'cd LMS-BE && docker build -t backend .'
            }
        }
        stage('run') {
            steps {
                sh 'docker run -dt --name lmsbe -p 9090:8080 backend'
            }
        }
    }
}
